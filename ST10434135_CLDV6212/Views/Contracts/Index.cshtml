@model IEnumerable<ST10434135_CLDV6212.Models.Contract>

@{
    ViewData["Title"] = "Contracts";
}

<h2>Contracts</h2>

<p>
    <a asp-action="Upload" class="btn btn-primary mb-3">Upload New Contract</a>
</p>

<table class="table custom-table shadow-sm">
    <thead>
        <tr>
            <th>File Name</th>
            <th>Uploaded On</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FileName</td>
                <td>@item.UploadedOn.ToString("yyyy-MM-dd HH:mm")</td>
                <td>
                    <a asp-action="Download"
                       asp-route-partitionKey="@item.PartitionKey"
                       asp-route-rowKey="@item.RowKey"
                       class="btn btn-sm btn-outline-success">
                        Download
                    </a>

                    <a asp-action="Delete"
                       asp-route-partitionKey="@item.PartitionKey"
                       asp-route-rowKey="@item.RowKey"
                       class="btn btn-sm btn-outline-danger">
                        Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
